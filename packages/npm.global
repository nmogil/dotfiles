# Global npm CLIs (installed via `npm install -g` in setup-linux.sh /
# setup-obsidian-sync.sh). One package per line. Used by `./dot packages check`.

@anthropic-ai/claude-code
@openai/codex
@earendil-works/pi-coding-agent@0.84.1
hunkdiff              # Linux path for hunk; macOS gets it from the Brewfile
obsidian-headless

# Pi is installed by `./dot pi install`, pinned there to the version verified
# with runtime-importing extensions. npm's standard global layout is required;
# the Vite+ global layout currently breaks runtime imports used by Pi extensions.
