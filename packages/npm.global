# Global npm CLIs (installed via `npm install -g` in setup-linux.sh /
# setup-obsidian-sync.sh). One package per line. Used by `./dot packages check`.

@anthropic-ai/claude-code
@openai/codex
hunkdiff
obsidian-headless

# Note: the Pi coding agent (@earendil-works/pi-coding-agent) is installed via
# Vite+ (`vp install -g ...`), NOT npm -g, so it is intentionally not listed
# here. See docs/pi-agent-setup.md and `./dot pi install`.
