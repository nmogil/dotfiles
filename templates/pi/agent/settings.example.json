{
  "$comment": "EXAMPLE ONLY. Copy to agent/settings.json (gitignored). Keep credentials in Pi auth or environment variables; never commit them.",
  "theme": "catppuccin-macchiato",
  "cloak": "cloak.json",
  "defaultProvider": "openai-codex",
  "defaultModel": "gpt-5.6-sol",
  "defaultThinkingLevel": "high",
  "enabledModels": [
    "openai-codex/gpt-5.6-sol",
    "anthropic/claude-opus-4-8",
    "anthropic/claude-sonnet-5",
    "anthropic/claude-fable-5"
  ],
  "$note": "The pinned subagent installer adds local package paths to the live packages array. Authenticate providers locally; this file contains no credentials."
}
